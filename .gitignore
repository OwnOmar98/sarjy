# env
.env
.env.local
.env.*.local

# node (web/)
node_modules/
.nuxt/
.output/
dist/

# python (agent/)
__pycache__/
*.pyc
.venv/
venv/
.ruff_cache/
*.egg-info/

# Krisp BVC noise-cancellation SDK writes this in the process's cwd —
# harmless runtime state, shows up wherever the agent/eval harness runs.
KMS/

# build intermediates
docs/PRD.html

# editors / os
.DS_Store
.vscode/

# Impeccable design skill — local session/tooling state (decision-page
# daemon logs, live-mode journals, generated review screenshots/comps).
# Ignoring the whole directory since there's no shared .impeccable/config.json
# to check in yet; if one gets added later to share hook/detector settings
# with teammates, carve out an exception for that one file specifically.
.impeccable/
